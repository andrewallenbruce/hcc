# Check if CREC indicates ESRD status

Check if CREC indicates ESRD status

## Usage

``` r
is_esrd_by_crec(crec)
```

## Arguments

- crec:

  description

## Value

logical

## Examples

``` r
is_esrd_by_crec(c("2", "3"))
#> [1] TRUE TRUE
```
