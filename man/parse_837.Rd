% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsers.R
\name{parse_837}
\alias{parse_837}
\title{X12-837 Health Care Claim Parser}
\usage{
parse_837(text)
}
\arguments{
\item{text}{\verb{<chr>} string of raw X12-837 text}
}
\value{
list
}
\description{
The 837 is the highest-volume transaction in US healthcare EDI. Every
commercial and public payer (Medicare, Medicaid, Tricare) consumes hundreds
of millions per month.
}
\details{
The entire provider-side billing revolves around its generation: from the EHR
(Epic, Cerner, Athenahealth, NextGen) or the PMS (Kareo, AdvancedMD,
eClinicalWorks), through a clearinghouse (Availity, Change Healthcare,
Waystar, Trizetto), with \verb{277CA}, \code{999}, and ultimately \code{835} returns.
\subsection{Common segments}{
\subsection{Transaction Set Header}{
\itemize{
\item \code{BHT}: Beginning of Hierarchical Transaction
\itemize{
\item Purpose \code{00} (Original)
\item Transaction type \code{CH} (Chargeable)
\item \code{RP} Reporting
\item Submitter \code{NM1*41}
\item Receiver \code{NM1*40}
}
}
}

\subsection{Detail: Three hierarchical levels}{
\itemize{
\item \verb{2000A} Billing Provider (Practice or Facility, with NPI, Taxonomy, TIN)
\item \verb{2000B} Subscriber Loop (Contract Holder)
\itemize{
\item \code{SBR}:
\itemize{
\item Subscriber Information with relationship code
\item Claim filing indicator (\code{CI} Commercial Insurance, \code{MB} Medicare Part B, \code{MC} Medicaid, etc.)
}
}
\item \verb{2000C} Patient Loop (the Patient when different from the Subscriber).
\itemize{
\item At the claim level, \code{CLM} Claim Information carries the patient account, total charge, facility code, claim frequency.
\item \code{HI} Health Care Information Codes carries ICD-10 diagnoses (qualifier \code{ABK} Principal Diagnosis, \code{ABF} Other Diagnosis).
\item The service section groups \code{LX} + \code{SV1} (Professional) / \code{SV2} (Institutional) / \code{SV3} (Dental) detailing each procedure with its CPT / HCPCS / CDT code, modifiers, units, charge, and service date via \code{DTP}.
}
}
}

\subsection{Summary}{

— a single \code{SE}
}

}
}
\examples{
purrr::map(hcc::x12_837[1], parse_837)
}
